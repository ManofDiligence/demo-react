import { createContext } from "react";
// first declare a variable which store the createContext()
// go to the App() to declare the coverage of useContext() in this app
// using <CartContext.Provider></CartContext.Provider>
export const CartContext = createContext();
